import ProfilePanel from "@/components/admin/ProfilePanel";

export const metadata = { title: "Account & Security | PIPRA Trading" };

export default function ProfilePage() {
  return <ProfilePanel />;
}
